<section id="expertise" class="expertise">
  <div class="expertise__content">
    <p class="badge">Why DProdz</p>
    <h2 class="section-title">A cross-functional team built for the next wave of digital innovation</h2>
    <p class="section-subtitle">
      From strategy to execution, every engagement is led by senior specialists in growth, security, and design. We collaborate
      closely with your teams to ship experiences that are beautiful, bulletproof, and measurable.
    </p>
    <div class="pillars">
      <article>
        <h3>Strategy obsessed</h3>
        <p>
          Data-backed roadmaps, experimental frameworks, and continuous learning loops ensure your investments always align with
          business goals.
        </p>
      </article>
      <article>
        <h3>Engineering first</h3>
        <p>
          Composable architectures, best-in-class frameworks, and DevSecOps pipelines designed to keep your stack secure and
          scalable.
        </p>
      </article>
      <article>
        <h3>Human impact</h3>
        <p>
          We craft brand touchpoints that resonate with Algerian and global audiences, creating inclusive experiences that build
          loyalty.
        </p>
      </article>
    </div>
  </div>
  <div class="expertise__media">
    <div class="panel panel--gradient">
      <h4>Acceleration stack</h4>
      <ul>
        <li>Enterprise-grade SEO labs</li>
        <li>Managed SOC and MDR partners</li>
        <li>Design systems & prototyping ops</li>
        <li>Custom web frameworks & automation</li>
      </ul>
    </div>
    <div class="panel">
      <h4>Certified expertise</h4>
      <p>Google Analytics, AWS, CISSP associates, Figma champion designers, and Agile coaches under one roof.</p>
    </div>
  </div>
</section>

<style>
  .expertise {
    display: grid;
    gap: clamp(3rem, 6vw, 5rem);
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    align-items: center;
    padding: clamp(4rem, 6vw, 6rem) 0;
  }

  .pillars {
    display: grid;
    gap: 1.6rem;
    margin-top: 2.4rem;
  }

  .pillars article {
    background: rgba(255, 255, 255, 0.04);
    padding: 1.6rem;
    border-radius: var(--radius-md);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .pillars h3 {
    margin-bottom: 0.5rem;
  }

  .pillars p {
    color: var(--color-muted);
    margin: 0;
  }

  .expertise__media {
    display: grid;
    gap: 1.5rem;
  }

  .panel {
    background: rgba(5, 11, 22, 0.92);
    padding: 2rem;
    border-radius: var(--radius-lg);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: var(--shadow-md);
  }

  .panel--gradient {
    background: linear-gradient(160deg, rgba(102, 51, 153, 0.18), rgba(5, 11, 22, 0.92));
  }

  .panel h4 {
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .panel ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.7rem;
  }

  .panel li {
    color: var(--color-muted);
    position: relative;
    padding-left: 1.2rem;
  }

  .panel li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }

  .panel p {
    margin: 0;
    color: var(--color-muted);
    line-height: 1.6;
  }
</style>
