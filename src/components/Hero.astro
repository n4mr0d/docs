<section class="hero" id="top">
  <div class="hero__content">
    <p class="badge">Algerian digital powerhouse</p>
    <h1>
      Transformative tech solutions that help you dominate<br />
      <span>search, security, and scale.</span>
    </h1>
    <p>
      DProdz is a multidisciplinary Algerian team delivering SEO, cybersecurity, design, and full-stack engineering to ignite
      your growth. We architect future-ready experiences backed by reliable hosting and consultancy.
    </p>
    <div class="hero__actions">
      <a class="button button-primary" href="#contact">Launch your project</a>
      <a class="button button-secondary" href="#services">Discover our services</a>
    </div>
    <dl class="hero__metrics">
      <div>
        <dt>12+</dt>
        <dd>Industries transformed</dd>
      </div>
      <div>
        <dt>95%</dt>
        <dd>Client retention rate</dd>
      </div>
      <div>
        <dt>&lt; 24h</dt>
        <dd>Incident response time</dd>
      </div>
    </dl>
  </div>
  <div class="hero__visual">
    <div class="visual-frame">
      <span aria-hidden="true" class="visual-frame__halo"></span>
      <span aria-hidden="true" class="visual-frame__grid"></span>
      <div class="visual-frame__content">
        <article class="visual-card visual-card--primary">
          <span class="visual-card__eyebrow">Active sprint</span>
          <strong>Innovation sprints</strong>
          <p>Rapid discovery workshops that uncover hidden opportunities and align your roadmap.</p>
          <span class="visual-chip">Next session · Monday 10:00</span>
        </article>

        <article class="visual-card visual-card--metric">
          <span class="visual-card__eyebrow">Security posture</span>
          <div class="visual-metric">
            <span class="visual-metric__value">98</span>
            <span class="visual-metric__scale">/100</span>
          </div>
          <p>Zero-trust readiness validated across cloud, app, and network layers.</p>
        </article>

        <article class="visual-card visual-card--log">
          <span class="visual-card__eyebrow">Live insights</span>
          <ul>
            <li><span>🚀</span><p>SEO campaign launch synced with security hardening checklist.</p></li>
            <li><span>🛡️</span><p>Edge firewall patch deployed — response time under 18 minutes.</p></li>
            <li><span>📈</span><p>Weekly growth review locked in with your product stakeholders.</p></li>
          </ul>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    gap: clamp(3rem, 5vw, 6rem);
    align-items: center;
    padding: clamp(4rem, 6vw, 6.5rem) 0 clamp(5rem, 7vw, 7rem);
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  }

  h1 {
    font-size: clamp(2.4rem, 3.5vw + 1rem, 4rem);
    line-height: 1.05;
    margin-bottom: 1.5rem;
  }

  h1 span {
    color: var(--color-primary);
  }

  p {
    font-size: 1.1rem;
    color: var(--color-muted);
    line-height: 1.7;
    max-width: 52ch;
  }

  .hero__actions {
    margin: 2.4rem 0;
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero__metrics {
    display: grid;
    grid-template-columns: repeat(3, minmax(120px, 1fr));
    gap: 1.5rem;
    margin: 0;
  }

  .hero__metrics div {
    padding: 1.2rem 1.4rem;
    background: rgba(255, 255, 255, 0.04);
    border-radius: var(--radius-sm);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
  }

  dt {
    font-size: 1.9rem;
    font-weight: 700;
    color: var(--color-text);
  }

  dd {
    margin: 0.35rem 0 0;
    font-size: 0.95rem;
    color: var(--color-muted);
  }

  .hero__visual {
    position: relative;
    min-height: 460px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .visual-frame {
    position: relative;
    width: min(440px, 100%);
    padding: 1px;
    border-radius: 26px;
    background: linear-gradient(135deg, rgba(0, 205, 255, 0.8), rgba(130, 80, 255, 0.4));
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.45);
    overflow: hidden;
  }

  .visual-frame__halo {
    position: absolute;
    inset: -40%;
    background: radial-gradient(circle at 30% 20%, rgba(0, 212, 255, 0.35), transparent 60%),
      radial-gradient(circle at 70% 70%, rgba(255, 130, 65, 0.25), transparent 55%);
    filter: blur(40px);
    opacity: 0.9;
  }

  .visual-frame__grid {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(rgba(255, 255, 255, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.08) 1px, transparent 1px);
    background-size: 36px 36px;
    opacity: 0.35;
  }

  .visual-frame__content {
    position: relative;
    display: grid;
    gap: 1rem;
    padding: clamp(1.8rem, 5vw, 2.4rem);
    background: rgba(5, 11, 22, 0.9);
    border-radius: 25px;
    backdrop-filter: blur(18px);
    z-index: 1;
  }

  .visual-card {
    border-radius: var(--radius-md);
    padding: 1.35rem 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(11, 18, 35, 0.85);
    box-shadow: 0 18px 40px rgba(5, 11, 22, 0.45);
    display: grid;
    gap: 0.5rem;
  }

  .visual-card strong {
    font-size: 1.2rem;
    letter-spacing: 0.01em;
  }

  .visual-card p {
    margin: 0;
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.72);
  }

  .visual-card__eyebrow {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(255, 255, 255, 0.55);
  }

  .visual-card--primary {
    position: relative;
    isolation: isolate;
  }

  .visual-card--primary::after {
    content: "";
    position: absolute;
    inset: -1px;
    border-radius: inherit;
    background: linear-gradient(135deg, rgba(0, 205, 255, 0.35), rgba(130, 80, 255, 0.25));
    opacity: 0.7;
    z-index: -1;
  }

  .visual-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.35rem 0.75rem;
    background: rgba(0, 205, 255, 0.14);
    border-radius: 999px;
    font-size: 0.8rem;
    letter-spacing: 0.02em;
    color: rgba(174, 241, 255, 0.95);
    width: fit-content;
  }

  .visual-card--metric {
    grid-template-columns: auto 1fr;
    align-items: center;
  }

  .visual-card--metric p {
    grid-column: 1 / -1;
  }

  .visual-metric {
    display: flex;
    align-items: baseline;
    gap: 0.3rem;
  }

  .visual-metric__value {
    font-size: 2.4rem;
    font-weight: 700;
    color: var(--color-primary);
  }

  .visual-metric__scale {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .visual-card--log ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.75rem;
  }

  .visual-card--log li {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.75rem;
    align-items: start;
  }

  .visual-card--log span {
    font-size: 1.1rem;
  }

  .visual-card--log p {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.7);
  }

  @media (max-width: 960px) {
    .hero__metrics {
      grid-template-columns: repeat(2, minmax(120px, 1fr));
    }

    .hero__visual {
      order: -1;
    }
  }

  @media (max-width: 640px) {
    .hero__metrics {
      grid-template-columns: repeat(1, minmax(120px, 1fr));
    }

    .hero__actions {
      flex-direction: column;
      align-items: stretch;
    }

    .visual-frame {
      width: 100%;
    }
  }
</style>
