<section class="hero" id="top">
  <div class="hero__content">
    <p class="badge">Algerian digital powerhouse</p>
    <h1>
      Transformative tech solutions that help you dominate<br />
      <span>search, security, and scale.</span>
    </h1>
    <p>
      DProdz is a multidisciplinary Algerian team delivering SEO, cybersecurity, design, and full-stack engineering to ignite
      your growth. We architect future-ready experiences backed by reliable hosting and consultancy.
    </p>
    <div class="hero__actions">
      <a class="button button-primary" href="#contact">Launch your project</a>
      <a class="button button-secondary" href="#services">Discover our services</a>
    </div>
    <dl class="hero__metrics">
      <div>
        <dt>12+</dt>
        <dd>Industries transformed</dd>
      </div>
      <div>
        <dt>95%</dt>
        <dd>Client retention rate</dd>
      </div>
      <div>
        <dt>&lt; 24h</dt>
        <dd>Incident response time</dd>
      </div>
    </dl>
  </div>
  <div class="hero__visual">
    <div class="orb"></div>
    <div class="glow"></div>
    <div class="grid-card">
      <strong>Innovation sprints</strong>
      <p>Rapid discovery workshops that uncover hidden opportunities and align your roadmap.</p>
    </div>
    <div class="grid-card alt">
      <strong>Security-first DNA</strong>
      <p>Proactive audits, zero-trust deployments, and resilient infrastructure out of the box.</p>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    gap: clamp(3rem, 5vw, 6rem);
    align-items: center;
    padding: clamp(4rem, 6vw, 6.5rem) 0 clamp(5rem, 7vw, 7rem);
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  }

  h1 {
    font-size: clamp(2.4rem, 3.5vw + 1rem, 4rem);
    line-height: 1.05;
    margin-bottom: 1.5rem;
  }

  h1 span {
    color: var(--color-primary);
  }

  p {
    font-size: 1.1rem;
    color: var(--color-muted);
    line-height: 1.7;
    max-width: 52ch;
  }

  .hero__actions {
    margin: 2.4rem 0;
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero__metrics {
    display: grid;
    grid-template-columns: repeat(3, minmax(120px, 1fr));
    gap: 1.5rem;
    margin: 0;
  }

  .hero__metrics div {
    padding: 1.2rem 1.4rem;
    background: rgba(255, 255, 255, 0.04);
    border-radius: var(--radius-sm);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
  }

  dt {
    font-size: 1.9rem;
    font-weight: 700;
    color: var(--color-text);
  }

  dd {
    margin: 0.35rem 0 0;
    font-size: 0.95rem;
    color: var(--color-muted);
  }

  .hero__visual {
    position: relative;
    min-height: 420px;
    display: grid;
    gap: 1.5rem;
  }

  .orb,
  .glow {
    position: absolute;
    inset: 0;
    border-radius: var(--radius-lg);
    mix-blend-mode: screen;
  }

  .orb {
    background: radial-gradient(circle at 30% 20%, rgba(0, 212, 255, 0.6), transparent 55%),
      radial-gradient(circle at 70% 60%, rgba(255, 138, 0, 0.35), transparent 45%);
    filter: blur(0px);
  }

  .glow {
    background: rgba(5, 11, 22, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: var(--shadow-lg);
  }

  .grid-card {
    position: relative;
    background: rgba(5, 11, 22, 0.85);
    padding: 2rem;
    border-radius: var(--radius-md);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: var(--shadow-md);
    z-index: 1;
    max-width: 320px;
  }

  .grid-card strong {
    display: block;
    font-size: 1.2rem;
    margin-bottom: 0.6rem;
  }

  .grid-card p {
    margin: 0;
    font-size: 1rem;
  }

  .grid-card.alt {
    justify-self: end;
    margin-top: 6rem;
  }

  @media (max-width: 960px) {
    .hero__metrics {
      grid-template-columns: repeat(2, minmax(120px, 1fr));
    }

    .grid-card.alt {
      justify-self: start;
      margin-top: 0;
    }
  }

  @media (max-width: 640px) {
    .hero__metrics {
      grid-template-columns: repeat(1, minmax(120px, 1fr));
    }

    .hero__actions {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>
