<footer class="footer">
  <div class="footer__brand">
    <a href="#top" aria-label="Back to top">
      <img src="/images/dpro-logo.svg" alt="DProdz logo" />
    </a>
    <p>
      DProdz is your Algerian partner for holistic growth—connecting SEO, cybersecurity, design, engineering, hosting, and IT
      consultancy under one collaborative banner.
    </p>
  </div>
  <div class="footer__cta">
    <h3>Let's imagine what we can build together</h3>
    <a class="button button-primary" href="#contact">Schedule a call</a>
  </div>
  <div class="footer__meta">
    <div class="footer-links">
      <a href="#services">Services</a>
      <a href="#expertise">Expertise</a>
      <a href="#testimonials">Testimonials</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </div>
    <p>© {new Date().getFullYear()} DProdz. Crafted with passion in Algeria.</p>
  </div>
</footer>

<style>
  .footer {
    margin-top: clamp(4rem, 7vw, 6rem);
    padding: clamp(3rem, 5vw, 4rem) 0 clamp(2.5rem, 4vw, 3.5rem);
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    display: grid;
    gap: 2.5rem;
  }

  .footer__brand {
    display: grid;
    gap: 1.2rem;
    text-align: left;
  }

  .footer__brand img {
    width: 80px;
  }

  .footer__brand p {
    color: var(--color-muted);
    margin: 0;
    max-width: 60ch;
  }

  .footer__cta {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    background: rgba(102, 51, 153, 0.08);
    border: 1px solid rgba(102, 51, 153, 0.25);
    border-radius: var(--radius-lg);
    padding: clamp(2rem, 4vw, 3rem);
    box-shadow: var(--shadow-sm);
  }

  .footer__cta h3 {
    margin: 0;
    font-size: clamp(1.6rem, 2.5vw, 2.2rem);
  }

  .footer__meta {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    color: var(--color-muted);
  }

  .footer__meta p {
    margin: 0;
  }

  @media (max-width: 640px) {
    .footer__brand {
      text-align: center;
      justify-items: center;
    }

    .footer__cta {
      text-align: center;
    }

    .footer__meta {
      align-items: center;
    }
  }
</style>
